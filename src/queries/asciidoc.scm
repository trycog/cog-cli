; Document title and section titles
(
  (document_title
    (line) @name) @definition.module
  (#strip! @name "\n$")
)

(
  (title1
    (line) @name) @definition.module
  (#strip! @name "\n$")
)

(
  (title2
    (line) @name) @definition.module
  (#strip! @name "\n$")
)

(
  (title3
    (line) @name) @definition.module
  (#strip! @name "\n$")
)

(
  (title4
    (line) @name) @definition.module
  (#strip! @name "\n$")
)

(
  (title5
    (line) @name) @definition.module
  (#strip! @name "\n$")
)

; Document attributes
(document_attr
  (attr_name) @name) @definition.property
