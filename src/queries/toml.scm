; Tables
(table
  (bare_key) @name) @definition.module

(table
  (quoted_key) @name) @definition.module

(table
  (dotted_key) @name) @definition.module

(table_array_element
  (bare_key) @name) @definition.module

(table_array_element
  (quoted_key) @name) @definition.module

(table_array_element
  (dotted_key) @name) @definition.module

; Key/value pairs
(pair
  (bare_key) @name) @definition.property

(pair
  (quoted_key) @name) @definition.property

(pair
  (dotted_key) @name) @definition.property
