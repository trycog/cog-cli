pub const config = @import("config.zig");
pub const client = @import("client.zig");
pub const commands = @import("commands.zig");
pub const tui = @import("tui.zig");
pub const protobuf = @import("protobuf.zig");
pub const scip = @import("scip.zig");
pub const scip_encode = @import("scip_encode.zig");
pub const code_intel = @import("code_intel.zig");
pub const path_matcher = @import("path_matcher.zig");
pub const settings = @import("settings.zig");
pub const paths = @import("paths.zig");
pub const fs_util = @import("fs_util.zig");
pub const extensions = @import("extensions.zig");
pub const help_text = @import("help_text.zig");
pub const debug = @import("debug.zig");
pub const curl = @import("curl.zig");
pub const tree_sitter_indexer = @import("tree_sitter_indexer.zig");
pub const agents = @import("agents.zig");
pub const mcp = @import("mcp.zig");
pub const hooks = @import("hooks.zig");
pub const watcher = @import("watcher.zig");
pub const bootstrap = @import("bootstrap.zig");
pub const debug_log = @import("debug_log.zig");
pub const repo_context = @import("repo_context.zig");
pub const session_context = @import("session_context.zig");
pub const memory_envelope = @import("memory_envelope.zig");
pub const sqlite = @import("sqlite.zig");
pub const memory_schema = @import("memory_schema.zig");
pub const memory = @import("memory.zig");
pub const observe = @import("observe.zig");
pub const credential_boundary = @import("credential_boundary.zig");
pub const index_manifest = @import("index_manifest.zig");
pub const git_state = @import("git_state.zig");
pub const update_check = @import("update_check.zig");

test {
    _ = index_manifest;
    _ = git_state;
    _ = update_check;
    _ = config;
    _ = client;
    _ = commands;
    _ = tui;
    _ = protobuf;
    _ = scip;
    _ = scip_encode;
    _ = code_intel;
    _ = path_matcher;
    _ = settings;
    _ = paths;
    _ = fs_util;
    _ = extensions;
    _ = help_text;
    _ = debug;
    _ = curl;
    _ = tree_sitter_indexer;
    _ = agents;
    _ = mcp;
    _ = hooks;
    _ = watcher;
    _ = bootstrap;
    _ = debug_log;
    _ = repo_context;
    _ = session_context;
    _ = memory_envelope;
    _ = sqlite;
    _ = memory_schema;
    _ = memory;
    _ = observe;
    _ = credential_boundary;
}
